[package]
name = "dino-game"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"

# This makes debug builds compile much faster
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3